% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stdz_district.R
\name{stdz_district}
\alias{stdz_district}
\title{Standardize District}
\usage{
stdz_district(
  full_district = character(),
  state,
  district,
  denote_sen = "00",
  denote_al = "01",
  sep = "-",
  old_sep = NA,
  sep_loc = NA
)
}
\arguments{
\item{full_district}{A district string or vector of district strings
(e.g., \code{"CA12"}, \code{"CA-12"}). If provided, \code{state} and
\code{district} are ignored.}

\item{state}{State abbreviation or vector of state abbreviations.}

\item{district}{District number or vector of district numbers.}

\item{denote_sen}{String used to represent Senate seats. Defaults to \code{"00"}.}

\item{denote_al}{String used to represent at-large districts. Defaults to \code{"01"}.}

\item{sep}{Separator for the output format. Also used to split input districts
unless \code{old_sep} is specified. Defaults to \code{"-"}.}

\item{old_sep}{Separator to split input districts on. Overrides \code{sep} for parsing.}

\item{sep_loc}{Character position at which to split state from district when
the input has no separator (e.g., \code{2} for \code{"CA12"}).}
}
\value{
A character vector of standardized district identifiers.
}
\description{
Normalize congressional district identifiers to a consistent format
(e.g., \code{"CA-01"}). Accepts either a combined district string or
separate state and district arguments.
}
\examples{
stdz_district(full_district = c("CA-12", "NY-al"))
# "CA-12" "NY-01"
stdz_district(state = c("CA", "NY"), district = c(12, 3))
# "CA-12" "NY-03"
stdz_district(full_district = "CA12", sep_loc = 2)
# "CA-12"
}
